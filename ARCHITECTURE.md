# 🏗️ Архитектурный обзор системы Прима-NEXT

## 📊 High-Level архитектура

### Общая схема

```
┌────────────────────────────────────────────────────────────────┐
│                        Internet Users                          │
└────────────────────────────┬─────────────────────────────────┘
                             │
                             ▼
        ┌────────────────────────────────────────────┐
        │   CDN / Load Balancer (nginx/CloudFlare)   │
        │   - Кэширование статики                    │
        │   - SSL/TLS termination                    │
        │   - DDoS защита                            │
        └────────────────────────────────────────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
        ▼                    ▼                    ▼
   ┌─────────┐          ┌──────────┐         ┌──────────┐
   │Frontend │          │   API    │         │Admin     │
   │(Vue 3)  │          │ Gateway  │         │Panel     │
   │SPA      │          │(Ocelot)  │         │          │
   └────┬────┘          └────┬─────┘         └────┬─────┘
        │                    │                    │
        └────────────────────┼────────────────────┘
                             │
                  (REST API + WebSocket)
                             │
        ┌────────────────────▼────────────────────┐
        │      Microservices Layer (Backend)      │
        ├────────────────────────────────────────┤
        │                                        │
        │  ┌────────────────────────────────┐   │
        │  │  Horizontal Pod Autoscaling    │   │
        │  │  (min 2, max 10 replicas)      │   │
        │  └────────────────────────────────┘   │
        │                                        │
        │  ┌─────────┐ ┌─────────┐ ┌─────────┐  │
        │  │  User   │ │ Company │ │ Search  │  │
        │  │ Service │ │ Service │ │ Service │  │
        │  └────┬────┘ └────┬────┘ └────┬────┘  │
        │       │           │           │        │
        │  ┌─────────┐ ┌─────────┐ ┌─────────┐  │
        │  │ Report  │ │Monitoring│ │ Graph   │  │
        │  │ Service │ │ Service │ │ Service │  │
        │  └────┬────┘ └────┬────┘ └────┬────┘  │
        │       │           │           │        │
        │  ┌─────────────────────────────────┐   │
        │  │  Shared Libraries & Utilities   │   │
        │  │  - RBAC, JWT, Logging, etc.    │   │
        │  └─────────────────────────────────┘   │
        │                                        │
        └────┬───────────────────────┬──────────┘
             │                       │
             ▼                       ▼
    ┌─────────────────┐    ┌────────────────────┐
    │  Data Layer     │    │ Message Queue      │
    ├─────────────────┤    ├────────────────────┤
    │ • PostgreSQL    │    │ • RabbitMQ         │
    │ • MongoDB       │    │ • Event Streaming  │
    │ • Elasticsearch │    │ • Job Processing   │
    │ • Redis         │    │ • Notifications    │
    │ • Neo4j         │    └────────────────────┘
    └─────────────────┘
```

---

## 🎯 Представления (Views) - User Interface

```
┌─────────────────────────────────────────────────────────┐
│                      Main Layout                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌────────────────────────────────────────────────┐   │
│  │              Header                            │   │
│  │  Logo | Search | Theme | Profile              │   │
│  └────────────────────────────────────────────────┘   │
│  ┌────────┐ ┌────────────────────────┐ ┌────────┐   │
│  │ Left   │ │                        │ │ Right  │   │
│  │ Sidebar│ │   Content Area         │ │Sidebar │   │
│  │        │ │                        │ │ Actions│   │
│  │ Nav    │ │  ┌────────────────┐    │ │        │   │
│  │ Items  │ │  │ Active View:   │    │ │ Quick  │   │
│  │        │ │  │                │    │ │ Actions│   │
│  │ Table  │ │  │ • SearchView   │    │ │        │   │
│  │ Chart  │ │  │ • TableView    │    │ │ Export │   │
│  │ Graph  │ │  │ • DashboardView│   │ │ Report │   │
│  │ Lists  │ │  │ • GraphView    │    │ │        │   │
│  │ Admin  │ │  │ • MapView      │    │ │ Monitor│   │
│  │        │ │  │ • EntityCard   │    │ │        │   │
│  │        │ │  └────────────────┘    │ │        │   │
│  └────────┘ └────────────────────────┘ └────────┘   │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### View Components

```
SearchView
├── SearchInput (автодополнение)
├── SearchSuggestions (выпадающий список)
└── RecentSearches

TableView
├── ToolBar (фильтры, сортировка)
├── DataTable
│   ├── Columns (конфигурируемые)
│   ├── Pagination
│   └── Selection (чекбоксы)
└── ContextMenu (быстрые действия)

DashboardView
├── FilterPanel
├── Charts (ECharts)
│   ├── Regional distribution
│   ├── Industry breakdown
│   └── Financial metrics
└── Drill-down capability

GraphView
├── GraphControls (zoom, pan, filter)
├── GraphVisualizer (Cytoscape/D3)
│   ├── Nodes (компании, люди, адреса)
│   ├── Edges (связи)
│   └── Legend
└── NodeDetails panel

MapView
├── MapProvider selector (OSM / Yandex)
├── MapControls (zoom, search)
├── MarkerClusters
├── HeatMap overlay
└── Legend & filters

EntityCard
├── CardHeader (название, статус, ИНН)
├── CarouselTabs (горизонтальная прокрутка)
│   ├── Summary (AI-саммари)
│   ├── General Info
│   ├── Finances
│   ├── Founders
│   ├── Risks
│   ├── Map
│   ├── History
│   ├── Connections
│   └── ...
└── RightSidebar (быстрые действия)
```

---

## 🛠️ Backend - Микросервисная архитектура

### Сервисы и их ответственность

```
┌──────────────────────────────────────────────────────┐
│              API Gateway (Ocelot)                    │
│ • Маршрутизация запросов                             │
│ • Аутентификация (JWT)                               │
│ • Rate limiting                                      │
│ • Request/Response трансформация                     │
│ • Логирование                                        │
└────────────┬─────────────────────────────────────────┘
             │
    ┌────────┼────────┐
    │        │        │
    ▼        ▼        ▼
┌────────┐ ┌────────┐ ┌────────┐
│ User   │ │Company │ │ Search │
│Service │ │Service │ │Service │
└────────┘ └────────┘ └────────┘
    │        │        │
    ▼        ▼        ▼
┌────────┐ ┌────────┐ ┌────────┐
│Report  │ │Monitor │ │ Graph  │
│Service │ │Service │ │Service │
└────────┘ └────────┘ └────────┘
    │        │        │
    └────────┼────────┘
             │
    ┌────────▼────────┐
    │  Shared Services │
    │ • Auth          │
    │ • Logging       │
    │ • Caching       │
    │ • Email         │
    └─────────────────┘
```

### User Service

```
Endpoints:
  POST   /api/users/register              # Регистрация
  POST   /api/users/login                 # Вход
  GET    /api/users/profile               # Профиль
  PUT    /api/users/profile               # Обновить профиль
  DELETE /api/users/{id}                  # Удалить аккаунт
  GET    /api/users/{id}/subscriptions    # Тарифы

Dependencies:
  • PostgreSQL (пользователи, роли)
  • Redis (сессии, JWT черный список)
  • Keycloak (OAuth2, SSO)
  • Email Service (уведомления)

Database:
  • Users table
  • Roles table
  • UserRoles junction
  • Subscriptions table
  • AuditLog table
```

### Company Service

```
Endpoints:
  GET    /api/companies                   # Список
  GET    /api/companies/{id}              # Деталь
  POST   /api/companies                   # Создать
  PUT    /api/companies/{id}              # Обновить
  DELETE /api/companies/{id}              # Удалить
  GET    /api/companies/{id}/financials   # Финансы
  GET    /api/companies/{id}/founders     # Учредители
  GET    /api/companies/{id}/connections # Связи

Dependencies:
  • MongoDB (объекты компаний)
  • PostgreSQL (метаданные)
  • Elasticsearch (индексирование)
  • Redis (кэш)
  • RabbitMQ (события)

Collections:
  • Companies
  • CompanyHistory
  • FinancialData
  • Founders
```

### Search Service

```
Endpoints:
  GET    /api/search                      # Полный поиск
  GET    /api/search/companies            # По компаниям
  GET    /api/search/founders             # По учредителям
  POST   /api/search/advanced             # Расширенный поиск
  GET    /api/search/suggestions          # Подсказки

Features:
  • Поиск по ИНН, ОГРН, названию, адресу
  • Фильтрация по статусу, ОКВЭД, региону
  • Фасетированный поиск
  • Автодополнение
  • Фонетический поиск

Backend:
  • Elasticsearch (индексы)
  • Redis (кэш результатов)
  • MongoDB (прямые запросы)
```

---

## 💾 Слой данных

### Database Schema

```
PostgreSQL (Users, System)
├── users
├── roles
├── user_roles
├── subscriptions
├── subscription_limits
├── audit_log
└── api_keys

MongoDB (Domain Data)
├── companies
│   ├── _id (ObjectId)
│   ├── inn (indexed)
│   ├── ogrn (indexed)
│   ├── name (indexed)
│   ├── status
│   ├── okved
│   ├── address
│   └── metadata
├── founders
├── financial_data
└── reports

Elasticsearch (Search Indices)
├── companies_v1
│   ├── inn (keyword)
│   ├── name (text + keyword)
│   ├── address (text)
│   └── okved (keyword)
└── founders_v1

Redis (Cache)
├── company:{id}
├── search:{query}
├── session:{token}
└── limits:{user_id}

Neo4j (Relationships)
├── Company nodes
├── Person nodes
├── Address nodes
└── Relationships (founder, office, beneficial_owner)
```

---

## 📡 Communication Patterns

### REST API

```
Frontend
  ↓ (HTTP/REST)
API Gateway (Ocelot)
  ↓
Service Layer
  ↓
Data Layer (PostgreSQL, MongoDB, Elasticsearch)
```

### Event-Driven (via RabbitMQ)

```
Service A (Company Service)
  → publishes: CompanyCreated, CompanyUpdated
     ↓
RabbitMQ (topic exchange)
  ↓
Service B (Search Service)  →  Updates Elasticsearch
Service C (Report Service)  →  Queues report generation
Service D (Monitor Service) →  Checks for alerts
```

### WebSocket (Real-time)

```
Frontend (Browser)
  ↔ WebSocket connection
Backend (SignalR Hub)
  ↔ Real-time updates
  • Report generation progress
  • Monitoring alerts
  • Live notifications
```

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│          Internet / External            │
└─────────────────────────────┬───────────┘
                              │
                              ▼
        ┌─────────────────────────────────┐
        │   WAF + DDoS Protection         │
        │   (CloudFlare / ModSecurity)    │
        └──────────────┬──────────────────┘
                       │
                       ▼
        ┌─────────────────────────────────┐
        │  TLS/SSL Termination (nginx)    │
        └──────────────┬──────────────────┘
                       │
                       ▼
        ┌─────────────────────────────────┐
        │  API Gateway Authentication     │
        │  • JWT validation               │
        │  • Rate limiting                │
        │  • Request signing              │
        └──────────────┬──────────────────┘
                       │
                       ▼
        ┌─────────────────────────────────┐
        │  RBAC Authorization             │
        │  • Policy-based access          │
        │  • Data-level permissions       │
        │  • Audit logging                │
        └──────────────┬──────────────────┘
                       │
                       ▼
        ┌─────────────────────────────────┐
        │  Application Layer              │
        │  • Input validation             │
        │  • SQL injection prevention     │
        │  • CORS configuration           │
        └──────────────┬──────────────────┘
                       │
                       ▼
        ┌─────────────────────────────────┐
        │  Database Security              │
        │  • Encrypted connections        │
        │  • Row-level security           │
        │  • Backup encryption            │
        └─────────────────────────────────┘
```

---

## 📊 Deployment Topology

### Development

```
docker-compose.yml
├── frontend (nginx on :3000)
├── api-gateway (:8000)
├── microservices (multiple ports)
├── postgresql (port 5432)
├── mongodb (port 27017)
├── redis (port 6379)
├── elasticsearch (port 9200)
├── rabbitmq (port 5672)
└── monitoring (prometheus, grafana)

All in one machine, all networks bridge
```

### Production (Kubernetes)

```
primanext namespace
├── Ingress (with TLS)
├── Frontend Service + Deployment (3 replicas)
├── API Gateway Service + Deployment (2 replicas)
├── Microservices (StatefulSets)
│   ├── User Service (2 replicas)
│   ├── Company Service (3 replicas)
│   ├── Search Service (3 replicas)
│   ├── Report Service (2 replicas)
│   ├── Monitor Service (2 replicas)
│   └── Graph Service (1 replica)
├── Persistent Storage
│   ├── PostgreSQL StatefulSet
│   ├── MongoDB StatefulSet
│   ├── Redis StatefulSet
│   ├── Elasticsearch StatefulSet
│   └── Neo4j Deployment
├── Message Queue (RabbitMQ StatefulSet)
├── Monitoring Stack
│   ├── Prometheus Deployment
│   ├── Grafana Deployment
│   └── Loki Deployment
└── Jobs & CronJobs (maintenance, backups)
```

---

## 🚀 Performance & Scalability

### Horizontal Scaling

```
Load Balancer (nginx/Ingress)
    ↓
Distributes load across pods

Company Service (HPA)
├── min replicas: 2
├── max replicas: 10
├── target CPU: 70%
├── target memory: 80%
└── scale up/down based on metrics
```

### Caching Strategy

```
Level 1 (Client)
  └── Browser cache (CSS, JS, images)

Level 2 (CDN)
  └── Static assets caching

Level 3 (Redis)
  ├── Session cache (ttl: 24h)
  ├── Company data cache (ttl: 1h)
  ├── Search results (ttl: 30m)
  └── Rate limit counters

Level 4 (Application)
  └── In-memory cache (IMemoryCache)

Database (persistent)
  ├── PostgreSQL (indexed queries)
  ├── Elasticsearch (pre-aggregated data)
  └── MongoDB (optimized collections)
```

---

## 📈 Monitoring Stack

```
┌──────────────────────────────────────────┐
│  Application & Infrastructure            │
│  • Services                              │
│  • Databases                             │
│  • Message Queues                        │
└────────────────┬─────────────────────────┘
                 │
        ┌────────▼────────┐
        │  Prometheus      │ (metrics collection)
        │  • Scrapes every 15s
        │  • Stores time-series data
        │  • Alerts based on rules
        └────────┬────────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
┌────────┐ ┌────────┐ ┌───────────┐
│Grafana │ │PagerDuty│ │Slack      │
│Dashboards│         │ │Alerts     │
└────────┘ └────────┘ └───────────┘

Loki (Logs)
└── Centralized logging
    ├── Application logs
    ├── Error logs
    ├── Audit logs
    └── Query by labels
```

---

## 🌐 Integrations

```
┌─────────────────────────────────────────┐
│   External Data Sources                 │
├─────────────────────────────────────────┤
│ • FNS API (EGRUL/EGRIP)                 │
│ • FSSP (долги)                          │
│ • Rosstat (статистика)                  │
│ • Arbitration Courts (суды)             │
│ • Government Procurement (госзакупки)   │
│ • Weather API (климатические данные)    │
└──────────────┬──────────────────────────┘
               │
      ┌────────▼────────┐
      │  Data Sync Job  │
      │  • Scheduled    │
      │  • Event-based  │
      │  • Cache result │
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │ MongoDB Storage │
      └─────────────────┘

┌─────────────────────────────────────────┐
│   CRM/ERP Integrations                  │
├─────────────────────────────────────────┤
│ • 1C (SOAP/REST)                        │
│ • amoCRM (API)                          │
│ • Bitrix24 (Webhook)                    │
│ • Yandex.Kassa (Payment)                │
└──────────────┬──────────────────────────┘
               │
      ┌────────▼────────┐
      │  Webhook Handler│
      │  • RabbitMQ     │
      │  • Processing   │
      └────────┬────────┘
               │
      ┌────────▼────────┐
      │ Backend Services│
      └─────────────────┘
```

---

## 📝 Ключевые показатели

| Метрика | Значение |
|---------|----------|
| **Response time (p95)** | < 2 сек |
| **Search latency** | < 500 мс |
| **Availability** | 99.9% SLA |
| **Backup frequency** | ежедневно |
| **Log retention** | 30 дней |
| **Metrics retention** | 15 дней |
| **Max concurrent users** | 1000+ |
| **Max requests/sec** | 10,000+ |

---

**Последнее обновление:** Декабрь 2024  
**Версия:** 2.0  
**Статус:** Production Ready ✅
