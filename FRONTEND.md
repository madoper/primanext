# 📱 Frontend Specification

## 📋 Обзор Frontend

### Технологический стек

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| **Framework** | Vue.js | 3.4+ |
| **Language** | TypeScript | 5.3+ |
| **State** | Pinia | 2.1+ |
| **Routing** | Vue Router | 4.2+ |
| **Build** | Vite | 5.0+ |
| **Styling** | TailwindCSS | 3.4+ |
| **Charts** | ECharts | 5.4+ |
| **Maps** | Leaflet + Yandex API | Latest |
| **Graphs** | Cytoscape.js | 3.28+ |
| **HTTP** | Axios | 1.6+ |
| **UI Components** | Headless UI | 1.7+ |
| **Testing** | Vitest, Playwright | Latest |
| **Linting** | ESLint + Prettier | Latest |

---

## 🏗️ Структура проекта

```
frontend/
├── src/
│   ├── components/
│   │   ├── common/
│   │   │   ├── Header.vue
│   │   │   ├── Sidebar.vue
│   │   │   ├── RightSidebar.vue
│   │   │   └── ...
│   │   ├── views/
│   │   │   ├── SearchView.vue
│   │   │   ├── TableView.vue
│   │   │   ├── DashboardView.vue
│   │   │   ├── GraphView.vue
│   │   │   ├── MapView.vue
│   │   │   └── EntityCard.vue
│   │   ├── shared/
│   │   │   ├── Button.vue
│   │   │   ├── Modal.vue
│   │   │   ├── Tooltip.vue
│   │   │   └── ...
│   │   └── sections/
│   │       ├── CompanyHeader.vue
│   │       ├── CarouselTabs.vue
│   │       ├── FinancialSection.vue
│   │       └── ...\n│   ├── stores/\n│   │   ├── auth.ts           # Pinia store для аутентификации\n│   │   ├── workspace.ts      # Управление состоянием приложения\n│   │   ├── search.ts         # Поиск и результаты\n│   │   ├── company.ts        # Данные компании\n│   │   └── ui.ts             # UI состояние (модалы, тосты)\n│   ├── services/\n│   │   ├── api/\n│   │   │   ├── client.ts     # Axios instance\n│   │   │   ├── user.ts       # User API\n│   │   │   ├── company.ts    # Company API\n│   │   │   ├── search.ts     # Search API\n│   │   │   └── ...\n│   │   ├── cache.ts          # Локальное кэширование\n│   │   └── notifications.ts  # Push notifications\n│   ├── router/\n│   │   ├── index.ts\n│   │   ├── routes.ts\n│   │   └── guards.ts\n│   ├── styles/\n│   │   ├── global.css        # Глобальные стили\n│   │   ├── variables.css     # CSS переменные\n│   │   └── tailwind.config.js\n│   ├── types/\n│   │   ├── models.ts         # TypeScript интерфейсы\n│   │   ├── api.ts            # API типы\n│   │   └── store.ts          # Store типы\n│   ├── utils/\n│   │   ├── formatters.ts     # Форматирование данных\n│   │   ├── validators.ts     # Валидация\n│   │   ├── date.ts           # Работа с датами\n│   │   └── common.ts         # Вспомогательные функции\n│   ├── App.vue\n│   └── main.ts\n├── tests/\n│   ├── unit/\n│   ├── integration/\n│   └── e2e/\n├── public/\n│   ├── images/\n│   ├── icons/\n│   └── favicon.ico\n├── vite.config.ts\n├── tsconfig.json\n├── tailwind.config.js\n├── vitest.config.ts\n├── playwright.config.ts\n└── package.json\n```\n\n---\n\n## 🎨 UI Layout\n\n### Main Layout Structure\n\n```vue\n<template>\n  <div class=\"main-layout\">\n    <!-- Header -->\n    <Header />\n    \n    <div class=\"layout-container\">\n      <!-- Left Sidebar -->\n      <Sidebar class=\"sidebar-left\" />\n      \n      <!-- Content Area -->\n      <main class=\"content-area\">\n        <!-- Router Views -->\n        <router-view />\n      </main>\n      \n      <!-- Right Sidebar -->\n      <RightSidebar class=\"sidebar-right\" />\n    </div>\n  </div>\n</template>\n```\n\n### Header Component\n\n```vue\n<template>\n  <header class=\"header\">\n    <div class=\"header-left\">\n      <Logo />\n      <SearchInput \n        v-model=\"searchQuery\" \n        @search=\"handleSearch\"\n        @focus=\"showSuggestions = true\"\n      />\n      <SearchSuggestions \n        v-if=\"showSuggestions\" \n        :suggestions=\"suggestions\"\n      />\n    </div>\n    \n    <div class=\"header-center\">\n      <Button @click=\"openAdvancedSearch\">Расширенный поиск</Button>\n      <Button @click=\"uploadList\">Загрузить список</Button>\n    </div>\n    \n    <div class=\"header-right\">\n      <ThemeSwitcher v-model=\"theme\" />\n      <PlanBadge :plan=\"user?.subscription?.plan\" />\n      <UserMenu :user=\"user\" />\n    </div>\n  </header>\n</template>\n\n<script setup lang=\"ts\">\nimport { ref, computed } from 'vue'\nimport { useAuthStore } from '@/stores/auth'\nimport { useSearchStore } from '@/stores/search'\n\nconst authStore = useAuthStore()\nconst searchStore = useSearchStore()\nconst user = computed(() => authStore.user)\n\nconst searchQuery = ref('')\nconst suggestions = ref([])\nconst showSuggestions = ref(false)\nconst theme = ref(localStorage.getItem('theme') || 'light')\n\nconst handleSearch = async (query: string) => {\n  if (!query.trim()) return\n  \n  await searchStore.performSearch(query)\n  showSuggestions.value = false\n}\n</script>\n```\n\n---\n\n## 📊 Views (Представления)\n\n### SearchView\n\n```vue\n<template>\n  <div class=\"search-view\">\n    <div class=\"search-header\">\n      <h1>Поиск компаний</h1>\n      <p>Введите реквизиты: ИНН, ОГРН, название</p>\n    </div>\n    \n    <div class=\"search-container\">\n      <SearchInput \n        v-model=\"query\" \n        placeholder=\"Введите ИНН, ОГРН или название\"\n        @search=\"handleSearch\"\n      />\n      <Button @click=\"showAdvanced = true\">Расширенный поиск</Button>\n    </div>\n    \n    <div v-if=\"isSearching\" class=\"loading\">\n      <Spinner />\n      <p>Идет поиск...</p>\n    </div>\n    \n    <div v-else-if=\"results.length\" class=\"results\">\n      <p class=\"results-count\">Найдено: {{ results.length }} компаний</p>\n      <ResultsCard \n        v-for=\"company in results\" \n        :key=\"company.id\"\n        :company=\"company\"\n        @click=\"navigateToCard(company.id)\"\n      />\n    </div>\n    \n    <div v-else class=\"empty-state\">\n      <p>Начните поиск компании</p>\n    </div>\n  </div>\n</template>\n\n<script setup lang=\"ts\">\nimport { ref, computed } from 'vue'\nimport { useRouter } from 'vue-router'\nimport { useSearchStore } from '@/stores/search'\n\nconst router = useRouter()\nconst searchStore = useSearchStore()\n\nconst query = ref('')\nconst showAdvanced = ref(false)\n\nconst isSearching = computed(() => searchStore.isLoading)\nconst results = computed(() => searchStore.results)\n\nconst handleSearch = async () => {\n  if (!query.value.trim()) return\n  await searchStore.performSearch(query.value)\n}\n\nconst navigateToCard = (companyId: string) => {\n  router.push({ name: 'EntityCard', params: { id: companyId } })\n}\n</script>\n```\n\n### TableView\n\n```vue\n<template>\n  <div class=\"table-view\">\n    <div class=\"table-toolbar\">\n      <ColumnSelector v-model=\"visibleColumns\" />\n      <FilterPanel v-model=\"filters\" />\n      <SortSelector v-model=\"sortBy\" />\n      <Button @click=\"exportTable\">Экспорт</Button>\n    </div>\n    \n    <div class=\"table-container\">\n      <table class=\"data-table\">\n        <thead>\n          <tr>\n            <th>\n              <input \n                type=\"checkbox\" \n                v-model=\"selectAll\"\n                @change=\"toggleSelectAll\"\n              />\n            </th>\n            <th v-for=\"col in visibleColumns\" :key=\"col.id\">\n              <button @click=\"sort(col.id)\">{{ col.label }}</button>\n            </th>\n          </tr>\n        </thead>\n        <tbody>\n          <tr \n            v-for=\"company in paginatedData\" \n            :key=\"company.id\"\n            :class=\"{ selected: isSelected(company.id) }\"\n            @click=\"navigateToCard(company.id)\"\n          >\n            <td>\n              <input \n                type=\"checkbox\" \n                :checked=\"isSelected(company.id)\"\n                @click.stop\n                @change=\"toggleSelect(company.id)\"\n              />\n            </td>\n            <td v-for=\"col in visibleColumns\" :key=\"col.id\">\n              {{ formatCell(company, col) }}\n            </td>\n          </tr>\n        </tbody>\n      </table>\n    </div>\n    \n    <Pagination \n      :page=\"page\" \n      :total=\"total\"\n      :page-size=\"pageSize\"\n      @change=\"page = $event\"\n    />\n  </div>\n</template>\n\n<script setup lang=\"ts\">\nimport { ref, computed } from 'vue'\nimport { useWorkspaceStore } from '@/stores/workspace'\nimport { useRouter } from 'vue-router'\n\nconst workspaceStore = useWorkspaceStore()\nconst router = useRouter()\n\nconst page = ref(1)\nconst pageSize = ref(20)\nconst visibleColumns = ref([\n  { id: 'name', label: 'Название' },\n  { id: 'inn', label: 'ИНН' },\n  { id: 'ogrn', label: 'ОГРН' },\n  { id: 'status', label: 'Статус' },\n])\n\nconst paginatedData = computed(() => {\n  const start = (page.value - 1) * pageSize.value\n  return workspaceStore.companies.slice(start, start + pageSize.value)\n})\n\nconst total = computed(() => workspaceStore.companies.length)\n\nconst navigateToCard = (id: string) => {\n  router.push({ name: 'EntityCard', params: { id } })\n}\n</script>\n```\n\n### EntityCard (Карточка компании)\n\n```vue\n<template>\n  <div class=\"entity-card-container\">\n    <EntityHeader :company=\"company\" />\n    \n    <CarouselTabs \n      :sections=\"sections\" \n      :active=\"activeTab\"\n      @select=\"activeTab = $event\"\n      @scroll=\"handleTabScroll\"\n    />\n    \n    <div class=\"card-content\">\n      <!-- Summary Section -->\n      <Section v-show=\"activeTab === 'summary'\" id=\"summary\">\n        <AIReportSection :company=\"company\" />\n      </Section>\n      \n      <!-- General Info Section -->\n      <Section v-show=\"activeTab === 'info'\" id=\"info\">\n        <GeneralInfoSection :company=\"company\" />\n      </Section>\n      \n      <!-- Finances Section -->\n      <Section v-show=\"activeTab === 'finances'\" id=\"finances\">\n        <FinancialSection :company=\"company\" />\n      </Section>\n      \n      <!-- Founders Section -->\n      <Section v-show=\"activeTab === 'founders'\" id=\"founders\">\n        <FoundersSection :company=\"company\" />\n      </Section>\n      \n      <!-- Risks Section -->\n      <Section v-show=\"activeTab === 'risks'\" id=\"risks\">\n        <RisksSection :risks=\"company.risks\" />\n      </Section>\n      \n      <!-- Map Section -->\n      <Section v-show=\"activeTab === 'map'\" id=\"map\">\n        <MapSection :address=\"company.address\" :coordinates=\"company.coordinates\" />\n      </Section>\n      \n      <!-- Connections Section -->\n      <Section v-show=\"activeTab === 'connections'\" id=\"connections\">\n        <GraphSection :company=\"company\" />\n      </Section>\n    </div>\n  </div>\n</template>\n\n<script setup lang=\"ts\">\nimport { ref, computed, onMounted } from 'vue'\nimport { useRoute } from 'vue-router'\nimport { useCompanyStore } from '@/stores/company'\n\nconst route = useRoute()\nconst companyStore = useCompanyStore()\n\nconst activeTab = ref('summary')\nconst sections = [\n  { id: 'summary', label: 'AI-Саммари' },\n  { id: 'info', label: 'Информация' },\n  { id: 'finances', label: 'Финансы' },\n  { id: 'founders', label: 'Учредители' },\n  { id: 'risks', label: 'Риски' },\n  { id: 'map', label: 'Карта' },\n  { id: 'connections', label: 'Связи' },\n]\n\nconst company = computed(() => companyStore.currentCompany)\n\nonMounted(async () => {\n  await companyStore.fetchCompany(route.params.id as string)\n})\n\nconst handleTabScroll = (tabId: string) => {\n  activeTab.value = tabId\n}\n</script>\n```\n\n### CarouselTabs Component\n\n```vue\n<template>\n  <div class=\"carousel-tabs\">\n    <div class=\"tabs-scroll-container\" ref=\"scrollContainer\">\n      <button \n        v-for=\"section in sections\" \n        :key=\"section.id\"\n        class=\"tab-button\"\n        :class=\"{ active: active === section.id }\"\n        @click=\"$emit('select', section.id)\"\n      >\n        {{ section.label }}\n      </button>\n    </div>\n  </div>\n</template>\n\n<script setup lang=\"ts\">\nimport { ref, watch, onMounted } from 'vue'\n\ninterface TabSection {\n  id: string\n  label: string\n}\n\ndefineProps<{\n  sections: TabSection[]\n  active: string\n}>()\n\ndefineEmits<{\n  select: [id: string]\n  scroll: [id: string]\n}>()\n\nconst scrollContainer = ref<HTMLElement>()\n\nonMounted(() => {\n  observeIntersection()\n})\n\nconst observeIntersection = () => {\n  // Use IntersectionObserver для автоматического переключения вкладок\n}\n</script>\n\n<style scoped>\n.carousel-tabs {\n  display: flex;\n  overflow-x: auto;\n  border-bottom: 1px solid #e5e7eb;\n  background: white;\n  position: sticky;\n  top: 0;\n  z-index: 10;\n}\n\n.tabs-scroll-container {\n  display: flex;\n  gap: 0.5rem;\n  padding: 0 1rem;\n}\n\n.tab-button {\n  padding: 0.75rem 1rem;\n  border: none;\n  background: none;\n  cursor: pointer;\n  white-space: nowrap;\n  border-bottom: 2px solid transparent;\n  transition: all 0.2s ease;\n}\n\n.tab-button.active {\n  border-bottom-color: #3b82f6;\n  color: #3b82f6;\n}\n</style>\n```\n\n---\n\n## 🏪 Pinia Stores\n\n### Auth Store\n\n```typescript\nimport { defineStore } from 'pinia'\n\nexport const useAuthStore = defineStore('auth', {\n  state: () => ({\n    user: null as User | null,\n    token: localStorage.getItem('auth_token'),\n    isLoading: false,\n  }),\n  \n  getters: {\n    isAuthenticated: (state) => !!state.token,\n    userRoles: (state) => state.user?.roles || [],\n  },\n  \n  actions: {\n    async login(email: string, password: string) {\n      this.isLoading = true\n      try {\n        const response = await userApi.login(email, password)\n        this.token = response.token\n        this.user = response.user\n        localStorage.setItem('auth_token', response.token)\n      } finally {\n        this.isLoading = false\n      }\n    },\n    \n    logout() {\n      this.token = null\n      this.user = null\n      localStorage.removeItem('auth_token')\n    },\n  },\n})\n```\n\n### Workspace Store\n\n```typescript\nexport const useWorkspaceStore = defineStore('workspace', {\n  state: () => ({\n    companies: [] as Company[],\n    selectedCompanyIds: new Set<string>(),\n    filters: {} as Record<string, any>,\n    undoStack: [] as WorkspaceState[],\n    redoStack: [] as WorkspaceState[],\n  }),\n  \n  getters: {\n    selectedCount: (state) => state.selectedCompanyIds.size,\n    isAnySelected: (state) => state.selectedCompanyIds.size > 0,\n  },\n  \n  actions: {\n    selectCompany(id: string) {\n      this.selectedCompanyIds.add(id)\n      this.saveHistory()\n    },\n    \n    deselectCompany(id: string) {\n      this.selectedCompanyIds.delete(id)\n      this.saveHistory()\n    },\n    \n    selectAll() {\n      this.companies.forEach(c => this.selectedCompanyIds.add(c.id))\n      this.saveHistory()\n    },\n    \n    clearSelection() {\n      this.selectedCompanyIds.clear()\n      this.saveHistory()\n    },\n    \n    saveHistory() {\n      this.undoStack.push(this.getState())\n      this.redoStack = []\n    },\n    \n    undo() {\n      if (this.undoStack.length > 0) {\n        this.redoStack.push(this.getState())\n        const previousState = this.undoStack.pop()!\n        this.restoreState(previousState)\n      }\n    },\n  },\n})\n```\n\n---\n\n## 🧪 Testing\n\n### Component Tests\n\n```typescript\nimport { describe, it, expect, beforeEach } from 'vitest'\nimport { mount } from '@vue/test-utils'\nimport SearchView from '@/components/views/SearchView.vue'\n\ndescribe('SearchView', () => {\n  let wrapper: any\n  \n  beforeEach(() => {\n    wrapper = mount(SearchView)\n  })\n  \n  it('renders search input', () => {\n    expect(wrapper.find('input[type=\"text\"]').exists()).toBe(true)\n  })\n  \n  it('performs search on input', async () => {\n    const input = wrapper.find('input[type=\"text\"]')\n    await input.setValue('7701102700')\n    await wrapper.vm.handleSearch()\n    \n    expect(wrapper.vm.isSearching).toBe(false)\n  })\n})\n```\n\n---\n\n**Последнее обновление:** Декабрь 2024  \n**Версия:** 2.0 Frontend Spec\n